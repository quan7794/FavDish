<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:elevation="@dimen/_99sdp"
    app:cardCornerRadius="@dimen/_5sdp"
    app:cardUseCompatPadding="true">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageButton
            android:id="@+id/btn_dish_option"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:color/transparent"
            android:layout_margin="@dimen/_5sdp"
            android:layout_gravity="top|end"
            android:elevation="@dimen/_10sdp"
            android:src="@drawable/ic_dish_option"
            android:foreground="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/dish_option" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/iv_dish_image"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/_1sdp"
                android:contentDescription="@string/dish"
                android:elevation="@dimen/_5sdp"
                android:scaleType="fitCenter"
                tools:src="@drawable/ic_image" />

            <TextView
                android:id="@+id/tv_dish_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/_10sdp"
                android:textSize="@dimen/_16ssp"
                android:textStyle="bold"
                tools:text="Dish Title" />
        </LinearLayout>
    </FrameLayout>
</androidx.cardview.widget.CardView>